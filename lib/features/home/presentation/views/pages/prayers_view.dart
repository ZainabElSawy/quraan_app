import 'package:flutter/material.dart';

class PrayersView extends StatelessWidget {
  const PrayersView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Prayers View"),
    );
  }
}
